#!/bin/sh

main() {
  # Dump environment
  export > /env

  echo "[sitepilot] Starting runit"
  /sitepilot/bin/runsvdir-start &
  wait "$!"
}

onterm() {
  echo "[sitepilot] Exiting..."
  pkill -SIGHUP runsvdir-start
  exit $?
}

main "$@"
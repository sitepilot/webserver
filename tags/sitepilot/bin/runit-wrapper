#!/bin/sh

main() {
  # Dump environment
  export > /env

  echo "[INFO] Starting runit"
  /sitepilot/bin/runsvdir-start &
  wait "$!"
}

onterm() {
  echo "[INFO] Exiting..."
  pkill -SIGHUP runsvdir-start
  exit $?
}

main "$@"